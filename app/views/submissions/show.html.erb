<% provide(:title, "Submission by #{@submission.author.name}") %>

<%= render 'submission_header' %>
<%= render 'grade_submission_form' %>
<%= render 'source_files' %>
<%= render 'output' %>

<%= render 'comments' %>
