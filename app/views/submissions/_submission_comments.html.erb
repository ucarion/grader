<% if @submission.comments.any? %>
  <ul>
    <% @submission.comments.each do |comment| %>
      <%= render comment if comment.content %>
    <% end %>
  </ul>
<% else %>
  <i>There are no comments for this submission.
<% end %>
