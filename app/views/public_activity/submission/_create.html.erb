<%= fa_icon 'file-o 3x fw', class: 'pull-left icon-fixed-width' %>
<div class="heading">
<% if current_user == activity.trackable.assignment.course.teacher %>
  <%= link_to activity.owner.name, activity.owner %>
  created a
  <%= link_to "new submission", activity.trackable %>
  for
  <%= link_to activity.trackable.assignment.name, activity.trackable.assignment %>
<% end %>
</div>
<div class="details">
  Status: <%= activity.trackable.status_as_string %>
</div>
