<% if @course.errors.any? %>
  <div class="alert alert-danger">
    Your form contains <%= pluralize(@course.errors.count, "error") %>.

    <ul>
      <% @course.errors.full_messages.each do |message| %>
        <li><%= message.capitalize %></li>
      <% end %>
    </ul>
  </div>
<% end %>
